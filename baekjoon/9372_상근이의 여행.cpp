#include <bits/stdc++.h>
using namespace std;

int T, N, M, x;

int main() {
    scanf("%d", &T);
    while (T--) {
        scanf("%d%d", &N, &M);
        printf("%d\n", N-1);
        while (M--) scanf("%d%d", &x, &x);
    }
}
