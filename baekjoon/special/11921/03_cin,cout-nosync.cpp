#include <bits/stdc++.h>
using namespace std;

int n, x;

int main() {
    ios::sync_with_stdio(false);
    cin >> n;
    cout << n << '\n';
    while (n--) {
        cin >> x;
        cout << x << '\n';
    }
}
